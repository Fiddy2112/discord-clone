/** @type {import('next').NextConfig} */
const nextConfig = {
    // config domain uploadthing.com
    images: {
        domains: [ 
            'uploadthing.com',
            'utfs.io'
        ]
        
    }
};

export default nextConfig;
